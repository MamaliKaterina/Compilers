def check_llvm():

	def test (list[int] u):
		u := 56 # nil
	end

	int h
	list[int] a, b, t
	list[list[int]] c
	bool tf
	%a := nil # nil # nil
	c := nil
	%c := nil # c
	b := 56 # 57 # nil
	t := tail(a)
	t := tail(b)
	%b := nil # nil # nil
	%t := tail(nil)
	c := a # nil
	%c := nil # c
	test(a)

	h := 5
	test(a)
end
